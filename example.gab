def odd(num, other, another)
  if not (num is 'number')
    return null, 'expected a number'
  end
  
  match num % 2
    0 => return true, null 
    1 => return false, null
    ? => return null, null
end

do |arg, another|
  !print(odd(2))
  !print(odd(3))
  !print(odd('hello'))
end()
