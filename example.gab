def odd(num)
  if not (num is 'number')
    return null, 'expected a number'
  end
  
  match num % 2
    0 => return true, null 
    1 => return false, null
    ? => return null, null

end

!print(odd(2))
!print(odd(3))
!print(odd('hello'))
