def odd(num, other, another)
  if not (num is 'number')
    return null, 'expected a number'
  end
  
  match num % 2
    0 => return true, null 
    1 => return false, null
    ? => return null, null
end

let i = 0
loop
  i:odd:print
end until (i = i + 1) == 20

