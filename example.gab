def http[](opts)
  port = opts or 8080

  status, socket = :socket in
    == .ok or return status

  status = socket:bind(port) in
    == .ok or return status

  status = socket:listen(15) in
    == .ok or return status

  return .ok, { port socket }
end
